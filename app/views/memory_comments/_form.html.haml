= form_for @memory_comment do |f|
  - if @memory_comment.errors.any?
    #error_explanation
      %h2= "#{pluralize(@memory_comment.errors.count, "error")} prohibited this memory_comment from being saved:"
      %ul
        - @memory_comment.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
