
%h1 Listing guest_book_entries

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @guest_book_entries.each do |guest_book_entry|
      %tr
        %td= link_to 'Show', guest_book_entry
        %td= link_to 'Edit', edit_guest_book_entry_path(guest_book_entry)
        %td= link_to 'Destroy', guest_book_entry, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Guest book entry', new_guest_book_entry_path
